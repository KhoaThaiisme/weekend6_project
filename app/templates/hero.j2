{% extends "base.j2" %}

{% block content %}
    <h2>Hero</h2>
  <form action="" method="post">
  {{ form.hidden_tag() }}
 <p>
    {{ form.name.label }}<br>
    {{ form.name(size=32) }}
  </p>
  <p>
    {{ form.description.label }}<br>
    {{ form.description(size=32) }}
  </p>
  <p>
    {{ form.comic_appeared_in.label }}<br>
    {{ form.comic_appeared_in(size=32) }}
  </p>
  <p>
    {{ form.super_powers.label }}<br>
    {{ form.super_powers(size=32) }}
 </p>
 <p>{{ form.submit() }}</p>
</form>
{% endblock %}